using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            int i = 1, contador = 0;
            String limitador = "";
            Boolean numeros = true;
            String[] a = { "1", "2", "3", "4", "5", "6", "7", "8", "9", "0", };

            while (numeros)
            {
                Console.Write("Escreva a Tabuada: ");
                limitador = Console.ReadLine();
                while( contador < a.Length)
                {
                    if(limitador <> a{ contador})
{

                    }
                    Console.WriteLine("Filho da puta! É pra escrever NÚMEROS!");
                }
            }
            
            while (i <= Convert.ToInt16(limitador))
            {
                Console.Write(i + " ");
                ++i;
                ++contador;
            }
        }
    }
}
